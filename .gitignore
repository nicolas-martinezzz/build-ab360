/node_modules
/.next/
/out/
/build
.env*.local
.env
.env.*
npm-debug.log*
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Agent skills from skills.sh (local Cursor only; not committed)
/.agents/
/skills-lock.json
/.cursor/

# Claude Code local config
/.claude/

# Harness Engineering — tracked in develop, ignored here to avoid polluting staging/main
# Do NOT add conventions.md, features.json, init.sh here — they are committed in develop
/agents.md
/history.md
/progress/

# Static HTML prototypes (implemented in Next.js)
/autodiagnostico.html
/self-assessment.html

# Deploy helper scripts (local only)
/scripts/deploy-staging.ps1
