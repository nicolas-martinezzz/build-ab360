/node_modules
/.next/
/out/
/build
.env*.local
.env
.env.*
npm-debug.log*
.DS_Store
*.tsbuildinfo
next-env.d.ts

# Agent skills from skills.sh (local Cursor only; not committed)
/.agents/
/skills-lock.json
/.cursor/

# Harness Engineering — tracked in develop only, never staging/main
# conventions.md, features.json, init.sh, .claude/agents/ are committed in develop
# agents.md, history.md, progress/ are local-only (never committed)
/agents.md
/history.md
/progress/

# Claude Code personal settings (local only, never committed)
/.claude/settings.local.json
/.claude/worktrees/

# Static HTML prototypes (implemented in Next.js)
/autodiagnostico.html
/self-assessment.html

# Deploy helper scripts (local only)
/scripts/deploy-staging.ps1

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Admin local dev (never deploy to prod)
/public/admin/.local
/public/admin/auth.local.php
/public/admin/seed.local.php
